import { Nunito, Amatic_SC } from 'next/font/google';

export const primaryFont = Nunito({ subsets: ['latin'], weight: '500' });
export const fontTitle = Amatic_SC({ subsets: ['latin'], weight: '700' });
